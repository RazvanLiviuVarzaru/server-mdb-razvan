name: PR forward mergeability

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]

permissions:
  contents: read

jobs:
  pr-forward-mergeability:
    uses: RazvanLiviuVarzaru/forward-branch-merge-check/.github/workflows/pr-forward-mergeability.yml@main
    with:
      tool_repository: RazvanLiviuVarzaru/forward-branch-merge-check
      tool_ref: main
      config_path: .github/forward-merge-check/forward-merge-chain.yml
    secrets:
      tool_repository_token: ${{ secrets.FORWARD_MERGE_CHECK_TOKEN }}
